model Division {
  id            String          @id @default(cuid())
  name          String          @unique
  districts      District[]
  centres        Centre[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model Category {
  id            String          @id @default(cuid())
  name          String          @unique
  description   String
  coverUrl      String
  slug          String          @unique
  Courses       Course[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([slug])
}

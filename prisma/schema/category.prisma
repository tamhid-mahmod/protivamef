model Category {
  id            String          @id @default(cuid())
  name          String          @unique
  description   String
  coverUrl      String

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
